
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // FORCE GLOBAL DEPLOYMENT IDENTIFIER: 20240520_FORCE_ALLOW_ALL_FINAL
    // Ensuring rules are re-deployed with absolute permissive access.

    match /{path=**} {
      allow read, write: if true;
    }
  }
}

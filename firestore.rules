
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // CRITICAL EMERGENCY REDEPLOY ID: 20240523_GLOBAL_RESET_V500
    // This unique ID forces a re-evaluation of security rules on the backend.

    // UNIVERSAL ACCESS GRANTED: Allows all read/write operations for development and MVP.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
